# Ranker

Ranker contains retrieval and reranking implementations.

MVP starts with local lexical matching and structured scoring. Embeddings and
LLM-assisted rerank can be added later behind the same interfaces.
