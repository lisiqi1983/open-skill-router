# Packages

Shared implementation packages live here. The goal is to keep product surfaces
thin and make the core behavior testable without shelling out to GitHub, MCP, or
agent-specific directories.

