{
  "name": "@openskillrouter/cli",
  "version": "0.0.0",
  "private": true,
  "description": "Command-line interface for Open Skill Router.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "skillrouter": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "pnpm typecheck",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@openskillrouter/core": "workspace:*",
    "commander": "^14.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.0"
  }
}
