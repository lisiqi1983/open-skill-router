# API

The API app is reserved for a future self-hosted or cloud index service.

MVP development should not depend on this app. The preferred early deployment
model is a static index snapshot published through GitHub Pages or releases.

