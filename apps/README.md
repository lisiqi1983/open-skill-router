# Apps

Runnable entry points live here.

- `cli`: local command-line workflow for indexing, recommending, installing, listing, and updating skills.
- `mcp-server`: MCP tools exposed to agent hosts.
- `api`: optional self-hosted HTTP API for shared recommendations and feedback.
- `indexer`: source indexing jobs for GitHub repositories and source manifests.
